from itertools import permutations

def perm(my_list):
    result = list(permutations(my_list))
    print("All permutations of your list")
    return result


my_list = []
for i in range(1, 5):
    my_list.append(i)
print(my_list)

list_permutations = perm(my_list)
print(list_permutations)
